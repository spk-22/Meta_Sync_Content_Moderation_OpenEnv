# Core OpenEnv library (from PyPI)
openenv[core]>=0.2.0

# Web framework and server
fastapi>=0.115.0
uvicorn>=0.24.0

# Install latest OpenEnv directly from GitHub (ensures you have newest features)
git+https://github.com/openai/openenv.git@main#egg=openenv